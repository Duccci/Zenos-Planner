# Requirements Database

The requirements database is stored in `.zeno/requirements.db` (SQLite).

On Windows, symbolic links require administrator privileges. Instead, access the database directly at:

```
.zeno/requirements.db
```

Or use the Zeno CLI commands:
- `zeno req list`
- `zeno req show <hash>`
- `zeno req deps <hash>`




